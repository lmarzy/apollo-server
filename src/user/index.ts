import { resolvers } from './data/user.resolvers';
import { typeDefs } from './data/user.type-defs';
import RandomUser from './data/user.providers';

export default { resolvers, typeDefs, RandomUser };
